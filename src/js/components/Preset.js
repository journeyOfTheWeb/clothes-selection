import { Component } from '../core/Component.js';

export default class Preset extends Component {
  constructor(props) {
    super({props});
  }

  render() {
    this.el.classList.add('preset-closet__item');
    this.el.append()
  }
}